{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "leprechaun.serviceAccountName" . }}
  namespace: {{ include "leprechaun.namespace" . }}
  labels:
    {{- include "leprechaun.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
